header
    a(href="/").header__logo
        i.fab.fa-youtube 
    nav
        ul 
            li
                a(href="/search")
                    i.fas.fa-search
            li
                a(href="/") Home
            if loggedIn
                li
                    a(href="/users/logout") Logout
                li
                    a(href="/users/edit") Edit Profile
                li 
                    a(href="/videos/upload") Upload Video
                li 
                    a(href=`/users/${loggedInUser._id}`) My Profile
                        span 😊
                        //- if loggedInUser.avartarUrl === ""
                        //-     span 😊
                        //- else    
                        //-     if loggedInUser.socialOnly.includes("github")
                        //-         img.header__avartar(src=loggedInUser.avatarUrl)
                        //-     else 
                        //-         img.header__avartar(src="/"+loggedInUser.avatarUrl)
                        
            else
                li
                    a(href="/login").header__btn Login
                li
                    a(href="/join").header__btn Join
                //- locals에 있는 값은 pug에서 바로 읽을수있다.